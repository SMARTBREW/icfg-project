export interface TestimonialProps {
  id: number;
  name: string;
  position: string;
  text: string;
  image: string;
}

export interface CertificateItem {
  id: number;
  title: string;
  image: string;
}
